# 宠物平台小程序前端开发需求文档

## 文档信息

- **项目名称**：宠物平台小程序
- **文档版本**：v3.0
- **后端API地址**：http://192.168.31.63:5000
- **更新日期**：2026-02-10

---

## 目录

1. [基础配置](#一基础配置)
2. [用户登录模块](#二用户登录模块)
3. [宠物管理模块](#三宠物管理模块)
4. [购物车模块](#四购物车模块)
5. [订单模块](#五订单模块)
6. [笔记模块](#六笔记模块)
7. [地址管理模块](#七地址管理模块)
8. [收藏模块](#八收藏模块)
9. [优惠券模块](#九优惠券模块)
10. [商品模块](#十商品模块)
11. [搜索模块](#十一搜索模块)
12. [文件上传模块](#十二文件上传模块)
13. [公共接口](#十三公共接口)
14. [注意事项](#十四注意事项)
15. [完整API列表](#十五完整api列表)

---

## 一、基础配置

### 1.1 API基础配置

在项目的配置文件中设置API基础地址：

```javascript
// config.js 或 app.js
const BASE_URL = 'http://192.168.31.63:5000'
```

### 1.2 请求拦截器配置

创建统一的请求拦截器，处理token和错误：

```javascript
// utils/request.js 或 api.js
const request = (options) => {
  const token = wx.getStorageSync('token')
  
  return new Promise((resolve, reject) => {
    wx.request({
      url: BASE_URL + options.url,
      method: options.method || 'GET',
      data: options.data || {},
      header: {
        'Content-Type': 'application/json',
        'Authorization': token ? `Bearer ${token}` : '',
        ...options.header
      },
      success: (res) => {
        if (res.statusCode === 200) {
          if (res.data.code === 0) {
            resolve(res.data)
          } else {
            wx.showToast({
              title: res.data.message || '请求失败',
              icon: 'none'
            })
            reject(res.data)
          }
        } else if (res.statusCode === 401) {
          wx.showToast({
            title: '登录已过期，请重新登录',
            icon: 'none'
          })
          wx.removeStorageSync('token')
          wx.navigateTo({
            url: '/pages/login/login'
          })
          reject(res.data)
        } else {
          wx.showToast({
            title: '网络请求失败',
            icon: 'none'
          })
          reject(res.data)
        }
      },
      fail: (err) => {
        wx.showToast({
          title: '网络连接失败',
          icon: 'none'
        })
        reject(err)
      }
    })
  })
}

module.exports = {
  request
}
```

---

## 二、用户登录模块

### 2.1 登录页面

**功能描述**：用户通过微信授权登录系统

**接口信息**：
- **接口地址**：`POST /api/user/login`
- **是否需要认证**：否

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| code | String | 是 | 微信登录code |
| nickname | String | 否 | 用户昵称 |
| avatarUrl | String | 否 | 头像URL |
| phone | String | 否 | 手机号 |

**请求示例**：

```javascript
// pages/login/login.js
Page({
  data: {
    userInfo: {}
  },

  onLoad() {
    this.getUserProfile()
  },

  getUserProfile() {
    wx.getUserProfile({
      desc: '用于完善用户资料',
      success: (res) => {
        this.setData({ userInfo: res.userInfo })
        this.doLogin()
      },
      fail: (err) => {
        console.log('获取用户信息失败', err)
      }
    })
  },

  doLogin() {
    wx.login({
      success: (res) => {
        if (res.code) {
          request({
            url: '/api/user/login',
            method: 'POST',
            data: {
              code: res.code,
              nickname: this.data.userInfo.nickName,
              avatarUrl: this.data.userInfo.avatarUrl
            }
          }).then(res => {
            wx.setStorageSync('token', res.data.token)
            wx.setStorageSync('userInfo', res.data.userInfo)
            wx.showToast({
              title: '登录成功',
              icon: 'success'
            })
            setTimeout(() => {
              wx.switchTab({
                url: '/pages/index/index'
              })
            }, 1500)
          })
        }
      }
    })
  }
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "登录成功",
  "data": {
    "userInfo": {
      "id": 1,
      "nickname": "微信用户",
      "avatar": "https://...",
      "phone": "13800138000"
    },
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

### 2.2 获取用户信息

**接口信息**：
- **接口地址**：`GET /api/user/info`
- **是否需要认证**：是

**请求示例**：

```javascript
request({
  url: '/api/user/info'
}).then(res => {
  this.setData({ userInfo: res.data })
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "nickname": "微信用户",
    "avatar": "https://...",
    "phone": "13800138000",
    "created_at": "2024-01-01 00:00:00"
  }
}
```

### 2.3 更新用户信息

**接口信息**：
- **接口地址**：`PUT /api/user/info`
- **是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| nickname | String | 否 | 新昵称 |
| phone | String | 否 | 手机号 |
| avatarUrl | String | 否 | 头像URL |

**请求示例**：

```javascript
request({
  url: '/api/user/info',
  method: 'PUT',
  data: {
    nickname: '新昵称',
    phone: '13800138000',
    avatarUrl: 'https://...'
  }
}).then(res => {
  wx.showToast({
    title: '更新成功',
    icon: 'success'
  })
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "id": 1,
    "nickname": "新昵称",
    "phone": "13800138000",
    "avatar": "https://..."
  }
}
```

---

## 三、宠物管理模块

### 3.1 获取宠物列表

**接口信息**：
- **接口地址**：`GET /api/pets`
- **是否需要认证**：是

**请求示例**：

```javascript
Page({
  data: {
    pets: []
  },

  onLoad() {
    this.loadPets()
  },

  loadPets() {
    request({
      url: '/api/pets'
    }).then(res => {
      this.setData({ pets: res.data })
    })
  }
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "旺财",
      "type": "dog",
      "breed": "金毛",
      "age": "3岁",
      "weight": "25kg",
      "gender": "公",
      "birthday": "2021-01-15",
      "avatar": "https://...",
      "healthStatus": "健康",
      "vaccineRecords": "已完成狂犬疫苗"
    }
  ]
}
```

### 3.2 添加宠物

**接口信息**：
- **接口地址**：`POST /api/pets`
- **是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | String | 是 | 宠物名称 |
| type | String | 是 | 宠物类型：dog/cat/rabbit等 |
| breed | String | 否 | 品种 |
| age | String | 否 | 年龄 |
| weight | String | 否 | 体重 |
| gender | String | 否 | 性别 |
| birthday | String | 否 | 生日 |
| avatar | String | 否 | 头像URL |
| healthStatus | String | 否 | 健康状态 |
| vaccineRecords | String | 否 | 疫苗记录 |

**请求示例**：

```javascript
request({
  url: '/api/pets',
  method: 'POST',
  data: {
    name: '宠物名称',
    type: 'dog',
    breed: '品种',
    age: '2岁',
    weight: '25kg',
    gender: '公',
    birthday: '2021-01-01',
    avatar: '头像URL',
    healthStatus: '健康',
    vaccineRecords: '疫苗记录'
  }
}).then(res => {
  wx.showToast({
    title: '添加成功',
    icon: 'success'
  })
  this.loadPets()
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "添加成功",
  "data": {
    "id": 1,
    "name": "旺财",
    "type": "dog"
  }
}
```

### 3.3 删除宠物

**接口信息**：
- **接口地址**：`DELETE /api/pets/{pet_id}`
- **是否需要认证**：是

**请求示例**：

```javascript
deletePet(e) {
  const petId = e.currentTarget.dataset.id
  wx.showModal({
    title: '确认删除',
    content: '确定要删除这只宠物吗？',
    success: (res) => {
      if (res.confirm) {
        request({
          url: `/api/pets/${petId}`,
          method: 'DELETE'
        }).then(res => {
          wx.showToast({
            title: '删除成功',
            icon: 'success'
          })
          this.loadPets()
        })
      }
    }
  })
}
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "删除成功",
  "data": null
}
```

---

## 四、购物车模块

### 4.1 获取购物车

**接口信息**：
- **接口地址**：`GET /api/cart`
- **是否需要认证**：是

**请求示例**：

```javascript
request({
  url: '/api/cart'
}).then(res => {
  this.setData({ cartItems: res.data })
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "product_id": 1,
      "quantity": 2,
      "selected": true,
      "name": "商品名称",
      "price": 89.00,
      "image": "https://...",
      "stock": 100
    }
  ]
}
```

### 4.2 添加商品到购物车

**接口信息**：
- **接口地址**：`POST /api/cart`
- **是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| productId | Number | 是 | 商品ID |
| quantity | Number | 否 | 数量，默认1 |

**请求示例**：

```javascript
addToCart(e) {
  const productId = e.currentTarget.dataset.id
  request({
    url: '/api/cart',
    method: 'POST',
    data: {
      productId: productId,
      quantity: 2
    }
  }).then(res => {
    wx.showToast({
      title: '已添加到购物车',
      icon: 'success'
    })
  })
}
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "已添加到购物车",
  "data": {
    "cartTotal": 5
  }
}
```

### 4.3 修改购物车

**接口信息**：
- **接口地址**：`PUT /api/cart/{cart_id}`
- **是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| quantity | Number | 否 | 数量 |
| selected | Boolean | 否 | 是否选中 |

**请求示例**：

```javascript
updateCartItem(e) {
  const { id, quantity, selected } = e.currentTarget.dataset
  request({
    url: `/api/cart/${id}`,
    method: 'PUT',
    data: {
      quantity: quantity,
      selected: selected
    }
  }).then(res => {
    this.loadCart()
  })
}
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "更新成功",
  "data": null
}
```

### 4.4 删除购物车项

**接口信息**：
- **接口地址**：`DELETE /api/cart/{cart_id}`
- **是否需要认证**：是

**请求示例**：

```javascript
deleteCartItem(e) {
  const cartId = e.currentTarget.dataset.id
  request({
    url: `/api/cart/${cartId}`,
    method: 'DELETE'
  }).then(res => {
    this.loadCart()
  })
}
```

### 4.5 清空购物车

**接口信息**：
- **接口地址**：`POST /api/cart/clear`
- **是否需要认证**：是

**请求示例**：

```javascript
clearCart() {
  wx.showModal({
    title: '确认清空',
    content: '确定要清空购物车吗？',
    success: (res) => {
      if (res.confirm) {
        request({
          url: '/api/cart/clear',
          method: 'POST'
        }).then(res => {
          this.setData({ cartItems: [] })
        })
      }
    }
  })
}
```

---

## 五、订单模块

### 5.1 获取订单列表

**接口信息**：
- **接口地址**：`GET /api/orders`
- **是否需要认证**：是

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| status | String | 否 | 订单状态 |
| page | Number | 否 | 页码，默认1 |
| pageSize | Number | 否 | 每页数量，默认10 |

**请求示例**：

```javascript
Page({
  data: {
    orders: [],
    currentTab: 'pending'
  },

  onLoad() {
    this.loadOrders()
  },

  onTabChange(e) {
    this.setData({ currentTab: e.detail.value })
    this.loadOrders()
  },

  loadOrders() {
    request({
      url: '/api/orders',
      data: {
        status: this.data.currentTab,
        page: 1,
        pageSize: 10
      }
    }).then(res => {
      this.setData({ orders: res.data })
    })
  }
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "orderNo": "OD20240115001234567",
      "totalAmount": 89.00,
      "status": "paid",
      "paymentMethod": "微信支付",
      "address": {
        "name": "张三",
        "phone": "13800138001",
        "province": "北京市",
        "city": "北京市",
        "district": "朝阳区",
        "detail": "望京街道101号"
      },
      "createdAt": "2024-01-15 10:00:00",
      "items": [
        {
          "id": 1,
          "productId": 1,
          "name": "商品名称",
          "spec": "1.5kg",
          "price": 89.00,
          "quantity": 1,
          "image": "https://..."
        }
      ]
    }
  ]
}
```

### 5.2 获取订单详情

**接口信息**：
- **接口地址**：`GET /api/orders/{order_id}`
- **是否需要认证**：是

**请求示例**：

```javascript
loadOrderDetail(orderId) {
  request({
    url: `/api/orders/${orderId}`
  }).then(res => {
    this.setData({ orderDetail: res.data })
  })
}
```

### 5.3 创建订单

**接口信息**：
- **接口地址**：`POST /api/orders`
- **是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| addressId | Number | 是 | 地址ID |
| items | Array | 是 | 商品列表 |
| couponId | Number | 否 | 优惠券ID |
| remark | String | 否 | 订单备注 |

**请求示例**：

```javascript
createOrder() {
  const selectedItems = this.data.cartItems.filter(item => item.selected)
  if (selectedItems.length === 0) {
    wx.showToast({
      title: '请选择商品',
      icon: 'none'
    })
    return
  }

  request({
    url: '/api/orders',
    method: 'POST',
    data: {
      addressId: this.data.selectedAddress.id,
      items: selectedItems.map(item => ({
        productId: item.product_id,
        quantity: item.quantity
      })),
      couponId: this.data.selectedCoupon?.id,
      remark: this.data.remark
    }
  }).then(res => {
    wx.showToast({
      title: '订单创建成功',
      icon: 'success'
    })
    wx.navigateTo({
      url: `/pages/order-detail/order-detail?id=${res.data.id}`
    })
  })
}
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "订单创建成功",
  "data": {
    "id": 1,
    "orderNo": "OD20240115001234567",
    "totalAmount": 89.00,
    "status": "pending"
  }
}
```

### 5.4 支付订单

**接口信息**：
- **接口地址**：`POST /api/orders/pay`
- **是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| orderId | Number | 是 | 订单ID |
| paymentMethod | String | 否 | 支付方式，默认微信支付 |

**请求示例**：

```javascript
payOrder(orderId) {
  request({
    url: '/api/orders/pay',
    method: 'POST',
    data: {
      orderId: orderId,
      paymentMethod: '微信支付'
    }
  }).then(res => {
    wx.showToast({
      title: '支付成功',
      icon: 'success'
    })
    this.loadOrderDetail(orderId)
  })
}
```

### 5.5 取消订单

**接口信息**：
- **接口地址**：`POST /api/orders/cancel`
- **是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| orderId | Number | 是 | 订单ID |

**请求示例**：

```javascript
cancelOrder(orderId) {
  wx.showModal({
    title: '确认取消',
    content: '确定要取消这个订单吗？',
    success: (res) => {
      if (res.confirm) {
        request({
          url: '/api/orders/cancel',
          method: 'POST',
          data: {
            orderId: orderId
          }
        }).then(res => {
          wx.showToast({
            title: '订单已取消',
            icon: 'success'
          })
          this.loadOrders()
        })
      }
    }
  })
}
```

---

## 六、笔记模块

### 6.1 获取笔记列表

**接口信息**：
- **接口地址**：`GET /api/notes`
- **是否需要认证**：是

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| category | String | 否 | 分类 |

**请求示例**：

```javascript
request({
  url: '/api/notes',
  data: {
    category: 'daily'
  }
}).then(res => {
  this.setData({ notes: res.data })
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "title": "笔记标题",
      "content": "笔记内容",
      "category": "daily",
      "images": ["https://..."],
      "tags": ["标签1", "标签2"],
      "createdAt": "2024-01-15 10:00:00"
    }
  ]
}
```

### 6.2 添加笔记

**接口信息**：
- **接口地址**：`POST /api/notes`
- **是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| title | String | 是 | 笔记标题 |
| content | String | 否 | 笔记内容 |
| category | String | 否 | 分类，默认daily |
| images | Array | 否 | 图片列表 |
| tags | Array | 否 | 标签列表 |

**请求示例**：

```javascript
addNote() {
  request({
    url: '/api/notes',
    method: 'POST',
    data: {
      title: this.data.title,
      content: this.data.content,
      category: this.data.category,
      images: this.data.images,
      tags: this.data.tags
    }
  }).then(res => {
    wx.showToast({
      title: '添加成功',
      icon: 'success'
    })
    this.loadNotes()
  })
}
```

### 6.3 删除笔记

**接口信息**：
- **接口地址**：`DELETE /api/notes/{note_id}`
- **是否需要认证**：是

**请求示例**：

```javascript
deleteNote(e) {
  const noteId = e.currentTarget.dataset.id
  wx.showModal({
    title: '确认删除',
    content: '确定要删除这条笔记吗？',
    success: (res) => {
      if (res.confirm) {
        request({
          url: `/api/notes/${noteId}`,
          method: 'DELETE'
        }).then(res => {
          this.loadNotes()
        })
      }
    }
  })
}
```

---

## 七、地址管理模块

### 7.1 获取地址列表

**接口信息**：
- **接口地址**：`GET /api/addresses`
- **是否需要认证**：是

**请求示例**：

```javascript
request({
  url: '/api/addresses'
}).then(res => {
  this.setData({ addresses: res.data })
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "张三",
      "phone": "13800138001",
      "province": "北京市",
      "city": "北京市",
      "district": "朝阳区",
      "detail": "望京街道101号",
      "isDefault": true,
      "createdAt": "2024-01-15 10:00:00"
    }
  ]
}
```

### 7.2 添加地址

**接口信息**：
- **接口地址**：`POST /api/addresses`
- **是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | String | 是 | 收货人姓名 |
| phone | String | 是 | 联系电话 |
| province | String | 是 | 省份 |
| city | String | 是 | 城市 |
| district | String | 是 | 区县 |
| detail | String | 是 | 详细地址 |
| isDefault | Boolean | 否 | 是否默认地址 |

**请求示例**：

```javascript
addAddress() {
  request({
    url: '/api/addresses',
    method: 'POST',
    data: {
      name: this.data.name,
      phone: this.data.phone,
      province: this.data.province,
      city: this.data.city,
      district: this.data.district,
      detail: this.data.detail,
      isDefault: this.data.isDefault
    }
  }).then(res => {
    wx.showToast({
      title: '添加成功',
      icon: 'success'
    })
    this.loadAddresses()
  })
}
```

### 7.3 更新地址

**接口信息**：
- **接口地址**：`PUT /api/addresses/{address_id}`
- **是否需要认证**：是

**请求示例**：

```javascript
updateAddress() {
  request({
    url: `/api/addresses/${this.data.addressId}`,
    method: 'PUT',
    data: {
      name: this.data.name,
      phone: this.data.phone,
      province: this.data.province,
      city: this.data.city,
      district: this.data.district,
      detail: this.data.detail,
      isDefault: this.data.isDefault
    }
  }).then(res => {
    wx.showToast({
      title: '更新成功',
      icon: 'success'
    })
    this.loadAddresses()
  })
}
```

### 7.4 删除地址

**接口信息**：
- **接口地址**：`DELETE /api/addresses/{address_id}`
- **是否需要认证**：是

**请求示例**：

```javascript
deleteAddress(e) {
  const addressId = e.currentTarget.dataset.id
  wx.showModal({
    title: '确认删除',
    content: '确定要删除这个地址吗？',
    success: (res) => {
      if (res.confirm) {
        request({
          url: `/api/addresses/${addressId}`,
          method: 'DELETE'
        }).then(res => {
          this.loadAddresses()
        })
      }
    }
  })
}
```

---

## 八、收藏模块

### 8.1 获取收藏列表

**接口信息**：
- **接口地址**：`GET /api/collections` 或 `GET /api/favorites`
- **是否需要认证**：是

**请求示例**：

```javascript
request({
  url: '/api/collections'
}).then(res => {
  this.setData({ favorites: res.data })
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "product_id": 1,
      "createdAt": "2024-01-15 10:00:00",
      "name": "商品名称",
      "price": 89.00,
      "image": "https://...",
      "original_price": 109.00,
      "stock": 100,
      "sales": 1250,
      "is_hot": true
    }
  ]
}
```

### 8.2 添加收藏

**接口信息**：
- **接口地址**：`POST /api/collections` 或 `POST /api/favorites`
- **是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| productId | Number | 是 | 商品ID |

**请求示例**：

```javascript
addFavorite(e) {
  const productId = e.currentTarget.dataset.id
  request({
    url: '/api/collections',
    method: 'POST',
    data: {
      productId: productId
    }
  }).then(res => {
    wx.showToast({
      title: '收藏成功',
      icon: 'success'
    })
  })
}
```

### 8.3 取消收藏

**接口信息**：
- **接口地址**：`DELETE /api/collections/{collection_id}` 或 `DELETE /api/favorites/{favorite_id}`
- **是否需要认证**：是

**请求示例**：

```javascript
removeFavorite(e) {
  const favoriteId = e.currentTarget.dataset.id
  request({
    url: `/api/collections/${favoriteId}`,
    method: 'DELETE'
  }).then(res => {
    wx.showToast({
      title: '取消收藏成功',
      icon: 'success'
    })
    this.loadFavorites()
  })
}
```

---

## 九、优惠券模块

### 9.1 获取优惠券列表

**接口信息**：
- **接口地址**：`GET /api/coupons`
- **是否需要认证**：是

**请求示例**：

```javascript
request({
  url: '/api/coupons'
}).then(res => {
  this.setData({ coupons: res.data })
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "新用户专享券",
      "amount": 20.00,
      "min_amount": 100.00,
      "expireTime": "2024-12-31 23:59:59",
      "status": "available",
      "createdAt": "2024-01-15 10:00:00"
    }
  ]
}
```

---

## 十、商品模块

### 10.1 获取商品列表

**接口信息**：
- **接口地址**：`GET /api/products`
- **是否需要认证**：否

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| limit | Number | 否 | 返回数量，默认10 |
| category | String | 否 | 分类 |
| isHot | Number | 否 | 是否热门（0/1） |
| keyword | String | 否 | 搜索关键词 |

**请求示例**：

```javascript
request({
  url: '/api/products',
  data: {
    limit: 10,
    category: '狗粮',
    isHot: 1,
    keyword: '搜索关键词'
  }
}).then(res => {
  this.setData({ products: res.data })
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "商品名称",
      "category": "狗粮",
      "price": 89.00,
      "original_price": 109.00,
      "image_url": "https://...",
      "description": "商品描述",
      "stock": 100,
      "sales": 1250,
      "is_hot": true
    }
  ],
  "count": 10
}
```

### 10.2 获取商品详情

**接口信息**：
- **接口地址**：`GET /api/products/{product_id}`
- **是否需要认证**：否

**请求示例**：

```javascript
loadProductDetail(productId) {
  request({
    url: `/api/products/${productId}`
  }).then(res => {
    this.setData({ product: res.data })
  })
}
```

---

## 十一、搜索模块

### 11.1 搜索

**接口信息**：
- **接口地址**：`GET /api/search`
- **是否需要认证**：否

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| keyword | String | 是 | 搜索关键词 |
| type | String | 否 | 搜索类型（product/note），默认product |
| page | Number | 否 | 页码，默认1 |
| pageSize | Number | 否 | 每页数量，默认10 |

**请求示例**：

```javascript
search() {
  request({
    url: '/api/search',
    data: {
      keyword: this.data.keyword,
      type: 'product',
      page: 1,
      pageSize: 10
    }
  }).then(res => {
    this.setData({ searchResults: res.data })
  })
}
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "success",
  "data": [...],
  "keyword": "狗粮",
  "type": "product"
}
```

---

## 十二、文件上传模块

### 12.1 上传图片

**接口信息**：
- **接口地址**：`POST /api/upload`
- **是否需要认证**：是

**支持的文件类型**：png, jpg, jpeg, gif
**最大文件大小**：16MB

**请求示例**：

```javascript
uploadImage() {
  wx.chooseImage({
    count: 1,
    sizeType: ['compressed'],
    sourceType: ['album', 'camera'],
    success: (res) => {
      const tempFilePath = res.tempFilePaths[0]
      
      wx.uploadFile({
        url: `${BASE_URL}/api/upload`,
        filePath: tempFilePath,
        name: 'file',
        header: {
          'Authorization': `Bearer ${wx.getStorageSync('token')}`
        },
        success: (uploadRes) => {
          const data = JSON.parse(uploadRes.data)
          if (data.code === 0) {
            console.log('上传成功', data.data.url)
            this.setData({ imageUrl: data.data.url })
          } else {
            wx.showToast({
              title: data.message || '上传失败',
              icon: 'none'
            })
          }
        },
        fail: (err) => {
          wx.showToast({
            title: '上传失败',
            icon: 'none'
          })
        }
      })
    }
  })
}
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "上传成功",
  "data": {
    "url": "/uploads/1234567890_image.jpg",
    "filename": "1234567890_image.jpg"
  }
}
```

---

## 十三、公共接口

### 13.1 健康检查

**接口信息**：
- **接口地址**：`GET /api/health`
- **是否需要认证**：否

**请求示例**：

```javascript
request({
  url: '/api/health'
}).then(res => {
  console.log('服务状态', res.data)
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "服务正常",
  "data": {
    "status": "running",
    "database": "connected",
    "timestamp": "2024-01-15 10:00:00"
  }
}
```

### 13.2 获取宠物分类

**接口信息**：
- **接口地址**：`GET /api/pets/categories`
- **是否需要认证**：否

**请求示例**：

```javascript
request({
  url: '/api/pets/categories'
}).then(res => {
  this.setData({ categories: res.data })
})
```

**响应数据格式**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "狗狗",
      "icon": "🐶",
      "sort_order": 1,
      "status": 1
    }
  ],
  "count": 3
}
```

---

## 十四、注意事项

### 14.1 认证流程

1. 用户首次进入，检查是否有token
2. 如果没有token，跳转到登录页
3. 登录成功后，保存token到本地存储
4. 所有需要认证的接口，在请求头中携带token
5. 如果收到401错误，清除token并跳转到登录页

**示例代码**：

```javascript
// app.js
App({
  onLaunch() {
    this.checkLogin()
  },

  checkLogin() {
    const token = wx.getStorageSync('token')
    if (!token) {
      wx.redirectTo({
        url: '/pages/login/login'
      })
    }
  }
})
```

### 14.2 Token管理

**保存Token**：

```javascript
// 登录成功后保存
wx.setStorageSync('token', token)
```

**请求时携带Token**：

```javascript
header: {
  'Authorization': `Bearer ${wx.getStorageSync('token')}`
}
```

**清除Token**：

```javascript
// 401错误时清除
wx.removeStorageSync('token')
```

### 14.3 错误处理

| 错误码 | 说明 | 处理方式 |
|--------|------|----------|
| 400 | 请求参数错误 | 提示用户检查输入 |
| 401 | 未认证或token过期 | 清除token，跳转登录页 |
| 404 | 资源不存在 | 提示用户资源不存在 |
| 500 | 服务器内部错误 | 提示用户稍后重试 |

### 14.4 数据格式

所有API响应统一格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

- `code`: 状态码，0表示成功，其他表示失败
- `message`: 响应消息
- `data`: 响应数据

### 14.5 服务器配置

- **基础URL**：`http://192.168.31.63:5000`
- **微信小程序域名白名单**：需要在微信小程序后台配置服务器域名
- **开发环境**：可以在"详情" -> "本地设置"中勾选"不校验合法域名"

### 14.6 开发模式说明

当前后端已启用开发模式，即使微信登录失败也会自动使用模拟登录，方便前端开发和测试。

---

## 十五、完整API列表

| 接口 | 方法 | 认证 | 说明 |
|------|------|------|------|
| /api/user/login | POST | 否 | 用户登录 |
| /api/user/info | GET | 是 | 获取用户信息 |
| /api/user/info | PUT | 是 | 更新用户信息 |
| /api/pets | GET | 是 | 获取宠物列表 |
| /api/pets | POST | 是 | 添加宠物 |
| /api/pets/{id} | DELETE | 是 | 删除宠物 |
| /api/cart | GET | 是 | 获取购物车 |
| /api/cart | POST | 是 | 添加到购物车 |
| /api/cart/{id} | PUT | 是 | 修改购物车 |
| /api/cart/{id} | DELETE | 是 | 删除购物车项 |
| /api/cart/clear | POST | 是 | 清空购物车 |
| /api/orders | GET | 是 | 获取订单列表 |
| /api/orders/{id} | GET | 是 | 获取订单详情 |
| /api/orders | POST | 是 | 创建订单 |
| /api/orders/pay | POST | 是 | 支付订单 |
| /api/orders/cancel | POST | 是 | 取消订单 |
| /api/notes | GET | 是 | 获取笔记列表 |
| /api/notes | POST | 是 | 添加笔记 |
| /api/notes/{id} | DELETE | 是 | 删除笔记 |
| /api/addresses | GET | 是 | 获取地址列表 |
| /api/addresses | POST | 是 | 添加地址 |
| /api/addresses/{id} | PUT | 是 | 更新地址 |
| /api/addresses/{id} | DELETE | 是 | 删除地址 |
| /api/collections | GET | 是 | 获取收藏列表 |
| /api/collections | POST | 是 | 添加收藏 |
| /api/collections/{id} | DELETE | 是 | 取消收藏 |
| /api/coupons | GET | 是 | 获取优惠券列表 |
| /api/products | GET | 否 | 获取商品列表 |
| /api/products/{id} | GET | 否 | 获取商品详情 |
| /api/search | GET | 否 | 搜索 |
| /api/upload | POST | 是 | 上传文件 |
| /api/health | GET | 否 | 健康检查 |
| /api/pets/categories | GET | 否 | 获取宠物分类 |

---

## 附录

### A. 订单状态说明

| 状态 | 说明 |
|------|------|
| pending | 待支付 |
| paid | 已支付 |
| shipping | 配送中 |
| completed | 已完成 |
| canceled | 已取消 |

### B. 笔记分类说明

| 分类 | 说明 |
|------|------|
| medical | 医疗 |
| food | 饮食 |
| train | 训练 |
| daily | 日常 |
| health | 健康 |

### C. 宠物类型说明

| 类型 | 说明 |
|------|------|
| dog | 狗 |
| cat | 猫 |
| rabbit | 兔子 |
| hamster | 仓鼠 |
| bird | 鸟 |
| fish | 鱼 |

---

**文档结束**

如有疑问，请联系后端开发人员。
